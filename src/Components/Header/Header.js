import React from 'react';

const Header = (props) => (
  <header>
    <h1>Explore</h1>
    {/* broken out into spans so phrase expands whole width */}
    <h2><span>N</span><span>e</span><span>w</span><span> </span><span>A</span><span>r</span><span>t</span><span>i</span><span>s</span><span>t</span><span>s</span></h2>
  </header>
);

export default Header;
